// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  VideoType,
  VideoCreateInput,
  VideoUpdateInput,
  VideoIdeaType,
  VideoIdeaCreateInput,
  VideoIdeaUpdateInput,
  CommentType,
  CommentCreateInput,
  CommentUpdateInput,
  AnalyticsSnapshotType,
  AnalyticsSnapshotCreateInput,
  AnalyticsSnapshotUpdateInput,
  GenreType,
  GenreCreateInput,
  GenreUpdateInput,
  ContentScriptType,
  ContentScriptCreateInput,
  ContentScriptUpdateInput,
  UploadQueueType,
  UploadQueueCreateInput,
  UploadQueueUpdateInput,
  ChatMessageType,
  ChatMessageCreateInput,
  ChatMessageUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// Video Client Functions
// ============================================================================

/**
 * List all Video records
 */
export async function getVideoList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<VideoType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/videos${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Video')
  }
  const json = await response.json()
  return (json.items || []) as VideoType[]
}

/**
 * Get a single Video by ID
 */
export async function getVideoById(args: { data: { id: string; userId?: string } }): Promise<VideoType> {
  const url = `${getApiBase()}/api/videos/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Video not found')
  }
  const json = await response.json()
  return json.data as VideoType
}

/**
 * Create a new Video
 */
export async function createVideo(args: { data: { input: VideoCreateInput; userId?: string } }): Promise<VideoType> {
  const url = `${getApiBase()}/api/videos`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Video')
  }
  const json = await response.json()
  return json.data as VideoType
}

/**
 * Update an existing Video
 */
export async function updateVideo(args: { data: { id: string; input: VideoUpdateInput; userId?: string } }): Promise<VideoType> {
  const url = `${getApiBase()}/api/videos/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Video')
  }
  const json = await response.json()
  return json.data as VideoType
}

/**
 * Delete a Video
 */
export async function deleteVideo(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/videos/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Video')
  }
  return { success: true }
}

// ============================================================================
// VideoIdea Client Functions
// ============================================================================

/**
 * List all VideoIdea records
 */
export async function getVideoIdeaList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<VideoIdeaType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/video-ideas${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list VideoIdea')
  }
  const json = await response.json()
  return (json.items || []) as VideoIdeaType[]
}

/**
 * Get a single VideoIdea by ID
 */
export async function getVideoIdeaById(args: { data: { id: string; userId?: string } }): Promise<VideoIdeaType> {
  const url = `${getApiBase()}/api/video-ideas/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'VideoIdea not found')
  }
  const json = await response.json()
  return json.data as VideoIdeaType
}

/**
 * Create a new VideoIdea
 */
export async function createVideoIdea(args: { data: { input: VideoIdeaCreateInput; userId?: string } }): Promise<VideoIdeaType> {
  const url = `${getApiBase()}/api/video-ideas`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create VideoIdea')
  }
  const json = await response.json()
  return json.data as VideoIdeaType
}

/**
 * Update an existing VideoIdea
 */
export async function updateVideoIdea(args: { data: { id: string; input: VideoIdeaUpdateInput; userId?: string } }): Promise<VideoIdeaType> {
  const url = `${getApiBase()}/api/video-ideas/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update VideoIdea')
  }
  const json = await response.json()
  return json.data as VideoIdeaType
}

/**
 * Delete a VideoIdea
 */
export async function deleteVideoIdea(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/video-ideas/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete VideoIdea')
  }
  return { success: true }
}

// ============================================================================
// Comment Client Functions
// ============================================================================

/**
 * List all Comment records
 */
export async function getCommentList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<CommentType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/comments${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Comment')
  }
  const json = await response.json()
  return (json.items || []) as CommentType[]
}

/**
 * Get a single Comment by ID
 */
export async function getCommentById(args: { data: { id: string; userId?: string } }): Promise<CommentType> {
  const url = `${getApiBase()}/api/comments/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Comment not found')
  }
  const json = await response.json()
  return json.data as CommentType
}

/**
 * Create a new Comment
 */
export async function createComment(args: { data: { input: CommentCreateInput; userId?: string } }): Promise<CommentType> {
  const url = `${getApiBase()}/api/comments`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Comment')
  }
  const json = await response.json()
  return json.data as CommentType
}

/**
 * Update an existing Comment
 */
export async function updateComment(args: { data: { id: string; input: CommentUpdateInput; userId?: string } }): Promise<CommentType> {
  const url = `${getApiBase()}/api/comments/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Comment')
  }
  const json = await response.json()
  return json.data as CommentType
}

/**
 * Delete a Comment
 */
export async function deleteComment(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/comments/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Comment')
  }
  return { success: true }
}

// ============================================================================
// AnalyticsSnapshot Client Functions
// ============================================================================

/**
 * List all AnalyticsSnapshot records
 */
export async function getAnalyticsSnapshotList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<AnalyticsSnapshotType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/analytics-snapshots${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list AnalyticsSnapshot')
  }
  const json = await response.json()
  return (json.items || []) as AnalyticsSnapshotType[]
}

/**
 * Get a single AnalyticsSnapshot by ID
 */
export async function getAnalyticsSnapshotById(args: { data: { id: string; userId?: string } }): Promise<AnalyticsSnapshotType> {
  const url = `${getApiBase()}/api/analytics-snapshots/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'AnalyticsSnapshot not found')
  }
  const json = await response.json()
  return json.data as AnalyticsSnapshotType
}

/**
 * Create a new AnalyticsSnapshot
 */
export async function createAnalyticsSnapshot(args: { data: { input: AnalyticsSnapshotCreateInput; userId?: string } }): Promise<AnalyticsSnapshotType> {
  const url = `${getApiBase()}/api/analytics-snapshots`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create AnalyticsSnapshot')
  }
  const json = await response.json()
  return json.data as AnalyticsSnapshotType
}

/**
 * Update an existing AnalyticsSnapshot
 */
export async function updateAnalyticsSnapshot(args: { data: { id: string; input: AnalyticsSnapshotUpdateInput; userId?: string } }): Promise<AnalyticsSnapshotType> {
  const url = `${getApiBase()}/api/analytics-snapshots/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update AnalyticsSnapshot')
  }
  const json = await response.json()
  return json.data as AnalyticsSnapshotType
}

/**
 * Delete a AnalyticsSnapshot
 */
export async function deleteAnalyticsSnapshot(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/analytics-snapshots/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete AnalyticsSnapshot')
  }
  return { success: true }
}

// ============================================================================
// Genre Client Functions
// ============================================================================

/**
 * List all Genre records
 */
export async function getGenreList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<GenreType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/genres${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Genre')
  }
  const json = await response.json()
  return (json.items || []) as GenreType[]
}

/**
 * Get a single Genre by ID
 */
export async function getGenreById(args: { data: { id: string; userId?: string } }): Promise<GenreType> {
  const url = `${getApiBase()}/api/genres/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Genre not found')
  }
  const json = await response.json()
  return json.data as GenreType
}

/**
 * Create a new Genre
 */
export async function createGenre(args: { data: { input: GenreCreateInput; userId?: string } }): Promise<GenreType> {
  const url = `${getApiBase()}/api/genres`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Genre')
  }
  const json = await response.json()
  return json.data as GenreType
}

/**
 * Update an existing Genre
 */
export async function updateGenre(args: { data: { id: string; input: GenreUpdateInput; userId?: string } }): Promise<GenreType> {
  const url = `${getApiBase()}/api/genres/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Genre')
  }
  const json = await response.json()
  return json.data as GenreType
}

/**
 * Delete a Genre
 */
export async function deleteGenre(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/genres/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Genre')
  }
  return { success: true }
}

// ============================================================================
// ContentScript Client Functions
// ============================================================================

/**
 * List all ContentScript records
 */
export async function getContentScriptList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ContentScriptType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/content-scripts${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ContentScript')
  }
  const json = await response.json()
  return (json.items || []) as ContentScriptType[]
}

/**
 * Get a single ContentScript by ID
 */
export async function getContentScriptById(args: { data: { id: string; userId?: string } }): Promise<ContentScriptType> {
  const url = `${getApiBase()}/api/content-scripts/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ContentScript not found')
  }
  const json = await response.json()
  return json.data as ContentScriptType
}

/**
 * Create a new ContentScript
 */
export async function createContentScript(args: { data: { input: ContentScriptCreateInput; userId?: string } }): Promise<ContentScriptType> {
  const url = `${getApiBase()}/api/content-scripts`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ContentScript')
  }
  const json = await response.json()
  return json.data as ContentScriptType
}

/**
 * Update an existing ContentScript
 */
export async function updateContentScript(args: { data: { id: string; input: ContentScriptUpdateInput; userId?: string } }): Promise<ContentScriptType> {
  const url = `${getApiBase()}/api/content-scripts/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ContentScript')
  }
  const json = await response.json()
  return json.data as ContentScriptType
}

/**
 * Delete a ContentScript
 */
export async function deleteContentScript(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/content-scripts/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ContentScript')
  }
  return { success: true }
}

// ============================================================================
// UploadQueue Client Functions
// ============================================================================

/**
 * List all UploadQueue records
 */
export async function getUploadQueueList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UploadQueueType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/upload-queues${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list UploadQueue')
  }
  const json = await response.json()
  return (json.items || []) as UploadQueueType[]
}

/**
 * Get a single UploadQueue by ID
 */
export async function getUploadQueueById(args: { data: { id: string; userId?: string } }): Promise<UploadQueueType> {
  const url = `${getApiBase()}/api/upload-queues/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'UploadQueue not found')
  }
  const json = await response.json()
  return json.data as UploadQueueType
}

/**
 * Create a new UploadQueue
 */
export async function createUploadQueue(args: { data: { input: UploadQueueCreateInput; userId?: string } }): Promise<UploadQueueType> {
  const url = `${getApiBase()}/api/upload-queues`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create UploadQueue')
  }
  const json = await response.json()
  return json.data as UploadQueueType
}

/**
 * Update an existing UploadQueue
 */
export async function updateUploadQueue(args: { data: { id: string; input: UploadQueueUpdateInput; userId?: string } }): Promise<UploadQueueType> {
  const url = `${getApiBase()}/api/upload-queues/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update UploadQueue')
  }
  const json = await response.json()
  return json.data as UploadQueueType
}

/**
 * Delete a UploadQueue
 */
export async function deleteUploadQueue(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/upload-queues/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete UploadQueue')
  }
  return { success: true }
}

// ============================================================================
// ChatMessage Client Functions
// ============================================================================

/**
 * List all ChatMessage records
 */
export async function getChatMessageList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ChatMessageType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/chat-messages${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ChatMessage')
  }
  const json = await response.json()
  return (json.items || []) as ChatMessageType[]
}

/**
 * Get a single ChatMessage by ID
 */
export async function getChatMessageById(args: { data: { id: string; userId?: string } }): Promise<ChatMessageType> {
  const url = `${getApiBase()}/api/chat-messages/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ChatMessage not found')
  }
  const json = await response.json()
  return json.data as ChatMessageType
}

/**
 * Create a new ChatMessage
 */
export async function createChatMessage(args: { data: { input: ChatMessageCreateInput; userId?: string } }): Promise<ChatMessageType> {
  const url = `${getApiBase()}/api/chat-messages`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ChatMessage')
  }
  const json = await response.json()
  return json.data as ChatMessageType
}

/**
 * Update an existing ChatMessage
 */
export async function updateChatMessage(args: { data: { id: string; input: ChatMessageUpdateInput; userId?: string } }): Promise<ChatMessageType> {
  const url = `${getApiBase()}/api/chat-messages/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ChatMessage')
  }
  const json = await response.json()
  return json.data as ChatMessageType
}

/**
 * Delete a ChatMessage
 */
export async function deleteChatMessage(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/chat-messages/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ChatMessage')
  }
  return { success: true }
}
